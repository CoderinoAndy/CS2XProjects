dicti = {
    "IBM": 2,
    "Smarts": 10, 
    "Stinks": 53.12,
    "FB": 10.67,
}

min_price = min(zip(dicti.values(), dicti.keys()))
max_price = max(zip(dicti.values(), dicti.keys()))

dicti.values(), dicti.keys()))
for price, name in prices_sorted:
    print(' ', name, price)
